# AI Challenge 2026 — Digital Detective

Решение задачи сегментации объектов, сгенерированных ИИ, на изображениях.

## Установка
pip install -r requirements.txt

## Обучение
python baseline.py train --data-dir /path/to/data

## Предсказание
python baseline.py predict --data-dir /path/to/data